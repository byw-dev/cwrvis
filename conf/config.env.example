# cwrvis 环境变量配置
# 复制本文件为 config.env 并按需修改，start.sh 启动时自动加载

PORT=8000

# 以下路径均相对于 app/ 目录（即 backend/ 的部署位置）
DB_PATH=../db/stats.db
STATIC_ROOT=../static
REPORT_DIR=../static/reports
GRID_DIR=../static/grid
